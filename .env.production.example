# Production Environment Configuration

# MongoDB Atlas Connection (Production Database)
# IMPORTANT: Use a separate production database, not your development database
MONGODB_URI=mongodb+srv://prod-username:prod-password@your-cluster.mongodb.net/production-db?appName=educationelly-db

# JWT Secret Key (MUST be different from development)
# Generate a new secure secret: openssl rand -base64 64
JWT_SECRET=your-production-secret-key-here-must-be-very-secure

# CORS Configuration
# Add your production domains (comma-separated)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://api.yourdomain.com

# React API URL (for client build)
# This should point to your production API endpoint
REACT_APP_API_URL=https://yourdomain.com/api

# Optional: Node Environment (set by docker-compose)
NODE_ENV=production

# Optional: Custom ports (usually not needed in production)
# SERVER_PORT=8080
# CLIENT_PORT=3000
