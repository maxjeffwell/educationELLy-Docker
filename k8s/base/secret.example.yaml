# IMPORTANT: This is a template file. DO NOT commit actual secrets!
# Create the actual secret using kubectl or sealed-secrets

# To create the secret from command line:
# kubectl create secret generic educationelly-secrets \
#   --from-literal=mongodb-uri='mongodb+srv://user:pass@cluster.mongodb.net/db' \
#   --from-literal=jwt-secret='your-jwt-secret-here' \
#   --namespace=educationelly

# Or encode values manually:
# echo -n 'your-mongodb-uri' | base64
# echo -n 'your-jwt-secret' | base64

apiVersion: v1
kind: Secret
metadata:
  name: educationelly-secrets
  namespace: educationelly
  labels:
    app: educationelly
type: Opaque
data:
  # Base64 encoded values (these are EXAMPLES - replace with your actual encoded secrets)
  mongodb-uri: <BASE64_ENCODED_MONGODB_URI>
  jwt-secret: <BASE64_ENCODED_JWT_SECRET>
